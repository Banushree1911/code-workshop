def count_divisibles(arr, n):
    count = 0
    for element in arr:
        if element % n == 0:
            count += 1
    return count


arr = [10, 25, 30, 45, 60, 75, 80]
n = 5
result = count_divisibles(arr, n)
print(f"Number of elements divisible by {n}: {result}")
